version: "3.9"

services:
  framework-core:
    image: rust:1.84
    working_dir: /workspace
    volumes:
      - ..:/workspace
    command: ["bash", "-lc", "cargo build -p diccy && cargo test -p diccy"]
