[package]
name = "pack-nm"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dicom-core = { path = "../dicom-core" }
pack-shared = { path = "../pack-shared" }

[features]
default = []
pack-nm = []
