[package]
name = "pack-rt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dicom-core = { path = "../dicom-core" }
dicom-pixel = { path = "../dicom-pixel" }

[features]
default = []
pack-rt = []
