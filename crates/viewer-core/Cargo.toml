[package]
name = "viewer-core"
version = "0.1.0"
edition = "2021"
publish = false

[features]
pack_measurement_physical_units = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
