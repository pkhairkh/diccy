[package]
name = "dicom-collab"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dicom-core = { path = "../dicom-core" }
dicom-audit = { path = "../dicom-audit" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
collab = []
crdt = []
