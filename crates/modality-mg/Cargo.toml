[package]
name = "modality-mg"
version = "0.1.0"
edition = "2021"
publish = false

[features]
modality-mg = []
tomosynthesis = []
cade-hooks = []

[dependencies]
dicom-core = { path = "../dicom-core" }
serde = { version = "1.0", features = ["derive"] }
