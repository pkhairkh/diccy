[package]
name = "pack-shared"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dicom-core = { path = "../dicom-core" }
dicom-pixel = { path = "../dicom-pixel", optional = true }

[features]
default = []
rendering = ["dicom-pixel"]
