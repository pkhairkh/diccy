[package]
name = "dicom-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dicom-core = { path = "../dicom-core" }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
