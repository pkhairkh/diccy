# Non-Primitive Class Policy Regression Fixtures

These fixtures are used to validate class-policy boundaries for non-primitives.

- `pass/` contains examples that should not produce violations.
- `fail/` contains examples expected to trigger class-policy violations.

